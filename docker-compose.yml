services:
  agent-ops:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-openai/gpt-4o-mini}
    restart: unless-stopped

